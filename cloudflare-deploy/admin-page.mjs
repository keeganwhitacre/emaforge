// Self-contained branding: the deployed Worker cannot load the builder's static assets.
export const faviconSvg = `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32">
  <rect x="1" y="1" width="30" height="30" rx="6" fill="#ffffff" stroke="#d9dde0" stroke-width="1.5"/>
  <g fill="none" stroke="#aa4035" stroke-width="1.9" stroke-linecap="round" stroke-linejoin="round">
    <path d="M5.5 9h5l2.5-3 3.5 6 2.5-3h7.5"/>
    <path d="M7 15h18l2 3h-7v4l3 4H9l3-4v-4H5z"/>
  </g>
</svg>`;

export const adminHtml = `<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width,initial-scale=1">
<title>EMA Forge study admin</title>
<link rel="icon" type="image/svg+xml" href="/favicon.svg">
<style>
:root{font-family:Inter,ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;color:#18202a;background:#f4f5f6;--ink:#18202a;--muted:#616c77;--line:#cbd1d6;--paper:#fff;--soft:#eef1f3;--accent:#a53c30;--accent-dark:#843027;--green:#237451;--amber:#8a6116;--red:#a02f25}*{box-sizing:border-box}body{margin:0;min-height:100vh;background:#f4f5f6;color:var(--ink)}button,input,textarea,select{font:inherit}button{cursor:pointer}.shell{min-height:100vh}.topbar{height:62px;background:#fff;border-bottom:1px solid var(--line);display:flex;align-items:center;padding:0 24px;gap:18px;position:sticky;top:0;z-index:20}.brand{display:flex;align-items:center;gap:10px;font-weight:750;white-space:nowrap}.mark{width:28px;height:28px;border:1px solid #b64a3e;color:var(--accent);display:grid;place-items:center;font-weight:800}.study-name{font-family:Georgia,serif;font-size:1.05rem;border-left:1px solid var(--line);padding-left:18px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.live-pill{margin-left:auto;display:inline-flex;align-items:center;gap:7px;color:var(--muted);font-size:.82rem}.live-dot{width:8px;height:8px;border-radius:50%;background:#929ba3}.live-dot.on{background:var(--green)}.layout{display:grid;grid-template-columns:220px minmax(0,1fr);max-width:1440px;margin:0 auto;min-height:calc(100vh - 62px)}nav{border-right:1px solid var(--line);padding:28px 18px;background:#f8f8f7}.nav-label{font-size:.69rem;text-transform:uppercase;letter-spacing:.1em;color:var(--muted);font-weight:750;margin:0 10px 12px}.nav-btn{display:block;width:100%;text-align:left;border:0;background:transparent;color:#3f4a54;padding:10px;border-radius:3px;margin:2px 0;font-weight:620}.nav-btn:hover{background:#ecefed}.nav-btn.active{background:#fff;color:var(--accent);box-shadow:inset 3px 0 0 var(--accent);border:1px solid #dfe3e6}.nav-note{border-top:1px solid var(--line);margin:28px 10px 0;padding-top:16px;font-size:.76rem;color:var(--muted);line-height:1.5}main{padding:36px;min-width:0}.view{display:none}.view.active{display:block}.page-head{display:flex;align-items:flex-end;justify-content:space-between;gap:20px;margin-bottom:26px}.page-head h1{font-family:Georgia,serif;font-size:2rem;margin:0 0 6px}.page-head p{margin:0;color:var(--muted);max-width:720px;line-height:1.5}.actions{display:flex;align-items:center;gap:9px;flex-wrap:wrap}.btn{display:inline-flex;align-items:center;justify-content:center;min-height:39px;padding:9px 14px;border-radius:3px;border:1px solid #aeb6bc;background:#fff;color:#26313a;font-weight:680;text-decoration:none}.btn:hover{border-color:#7f8991;background:#f8f8f7}.btn.primary{background:var(--accent);border-color:var(--accent);color:#fff}.btn.primary:hover{background:var(--accent-dark)}.btn.danger{color:var(--red);border-color:#d9aaa4}.btn.small{min-height:32px;padding:6px 10px;font-size:.78rem}.btn:disabled{opacity:.5;cursor:not-allowed}.grid{display:grid;gap:16px}.kpis{grid-template-columns:repeat(4,minmax(0,1fr));margin-bottom:18px}.card{background:var(--paper);border:1px solid var(--line);border-radius:4px;padding:20px}.card h2{font-family:Georgia,serif;font-size:1.15rem;margin:0 0 5px}.card p{color:var(--muted);line-height:1.5;margin:4px 0 15px}.kpi-label{font-size:.72rem;text-transform:uppercase;letter-spacing:.08em;color:var(--muted);font-weight:740}.kpi-value{font-size:1.8rem;font-weight:760;margin-top:8px}.kpi-note{font-size:.76rem;color:var(--muted);margin-top:3px}.destinations{grid-template-columns:repeat(3,minmax(0,1fr));margin-bottom:18px}.destination{display:flex;flex-direction:column;min-height:172px}.destination .tag{font-size:.68rem;text-transform:uppercase;letter-spacing:.08em;color:var(--accent);font-weight:780}.destination h2{margin-top:9px}.destination .actions{margin-top:auto}.warning{border:1px solid #d5b46e;background:#fffaf0;color:#664b18;padding:13px 15px;font-size:.84rem;line-height:1.5;margin-bottom:18px}.okbox{border:1px solid #91bba6;background:#f2faf6;color:#1e5d43;padding:13px 15px;font-size:.84rem;line-height:1.5;margin-bottom:18px}.split{display:grid;grid-template-columns:minmax(0,1.3fr) minmax(300px,.7fr);gap:18px}.field{display:grid;gap:7px;margin:14px 0}.field label{font-size:.79rem;font-weight:720}.field small{color:var(--muted);line-height:1.45}.field input,.field textarea,.field select{width:100%;border:1px solid #aeb6bc;border-radius:3px;background:#fff;color:var(--ink);padding:10px 11px;outline:none}.field input:focus,.field textarea:focus,.field select:focus{border-color:var(--accent);box-shadow:0 0 0 2px rgba(165,60,48,.12)}.field textarea{min-height:92px;resize:vertical}.inline-fields{display:grid;grid-template-columns:1fr 1fr 1.2fr 1fr auto;gap:10px;align-items:end}.invite-fields{grid-template-columns:1.2fr .6fr 1fr auto}.table-wrap{overflow:auto;border:1px solid var(--line);border-radius:3px;background:#fff}table{width:100%;border-collapse:collapse;min-width:660px}th,td{text-align:left;padding:10px 12px;border-bottom:1px solid #e3e6e8;font-size:.8rem}th{font-size:.69rem;text-transform:uppercase;letter-spacing:.07em;color:var(--muted);background:#f7f8f8}tr:last-child td{border-bottom:0}.status-badge{display:inline-flex;align-items:center;padding:3px 7px;border-radius:999px;font-size:.69rem;font-weight:740;background:#e9edef;color:#4d5963}.status-badge.good{background:#e2f2e9;color:#236443}.status-badge.warn{background:#fff1ce;color:#765411}.status-badge.bad{background:#f8e3e0;color:#8d3128}.bars{display:grid;gap:9px}.bar-row{display:grid;grid-template-columns:120px minmax(0,1fr) 44px;gap:10px;align-items:center;font-size:.8rem}.bar-track{height:9px;background:#e7eaec;overflow:hidden}.bar-fill{height:100%;background:#4f6fbd}.empty{padding:32px;text-align:center;color:var(--muted);border:1px dashed var(--line);background:#fafafa}.message{min-height:1.3em;margin-top:10px;color:var(--muted);font-size:.82rem}.message.good{color:var(--green)}.message.bad{color:var(--red)}.setup-grid{display:grid;grid-template-columns:1fr 1fr;gap:18px}.quiet{color:var(--muted);font-size:.79rem;line-height:1.55}.code{font-family:ui-monospace,SFMono-Regular,Menlo,monospace;font-size:.78rem;background:#eef0f1;padding:2px 5px}.lock-screen{min-height:100vh;display:grid;place-items:center;padding:24px;background:#f4f5f6}.lock-box{width:min(470px,100%);background:#fff;border:1px solid var(--line);border-top:4px solid var(--accent);padding:28px}.lock-box h1{font-family:Georgia,serif;margin:0 0 8px}.lock-box p{color:var(--muted);line-height:1.55}.lock-box .btn{width:100%;margin-top:8px}.hidden{display:none!important}@media(max-width:980px){.layout{grid-template-columns:1fr}nav{border-right:0;border-bottom:1px solid var(--line);padding:10px 16px;display:flex;gap:5px;overflow:auto}.nav-label,.nav-note{display:none}.nav-btn{width:auto;white-space:nowrap}.nav-btn.active{box-shadow:inset 0 -3px 0 var(--accent)}main{padding:24px}.kpis,.destinations{grid-template-columns:repeat(2,minmax(0,1fr))}.split{grid-template-columns:1fr}.inline-fields{grid-template-columns:1fr 1fr}}@media(max-width:620px){.topbar{padding:0 14px}.study-name{display:none}main{padding:20px 14px}.page-head{align-items:flex-start;flex-direction:column}.kpis,.destinations,.setup-grid,.inline-fields{grid-template-columns:1fr}.card{padding:16px}.live-pill span:last-child{display:none}}
/* The admin lives on the researcher's Worker, so its builder styling is self-contained. */
:root{font-family:"TX-02","Instrument Sans",ui-sans-serif,system-ui,sans-serif;--ink:#1f2328;--muted:#4a5562;--line:#d0d7de;--paper:#fff;--soft:#eef1f4;--accent:#a63f35;--accent-dark:#8d342c;--green:#237451}
body,.lock-screen{background:#fff;font-size:13px;line-height:1.5}
.topbar{height:64px;padding:0 20px;gap:20px;box-shadow:0 1px 0 rgba(31,35,40,.04)}.layout{max-width:none;min-height:calc(100vh - 64px);grid-template-columns:210px minmax(0,1fr)}
.brand{font-size:14px;font-weight:700;letter-spacing:-.01em;gap:9px}.mark{width:24px;height:24px;border-color:var(--line);border-radius:3px}.mark svg{width:15px;height:15px}.study-name{font-family:inherit;font-size:15px;font-weight:700;padding-left:20px}
nav{background:var(--soft);padding:28px 14px 18px}.nav-label{margin:0 13px 10px;color:#6e7781;font-size:12px;font-weight:500;letter-spacing:0;text-transform:none}.nav-btn{border-radius:0;padding:10px 13px;font-size:13px;font-weight:500}.nav-btn.active{color:var(--accent);background:#fff;border:1px solid var(--line);box-shadow:inset 3px 0 0 var(--accent);font-weight:650}.nav-note{margin:24px 13px 0}
main{max-width:1100px;width:100%;margin:0 auto;padding:30px 36px 70px}.page-head{margin-bottom:20px}.page-head h1,.lock-box h1{font-family:inherit;font-size:27px;line-height:1.2;font-weight:700;letter-spacing:-.02em}.page-head p{font-size:13px}.card{border-radius:4px;padding:16px}.card h2{font-family:inherit;font-size:16px;font-weight:700;letter-spacing:0}.card p{font-size:12px;line-height:1.55}.grid{gap:12px}.setup-grid,.split{gap:14px}.kpis,.destinations{margin-bottom:14px}.destination{min-height:155px;border-top:2px solid var(--ink)}.destination:first-child{border-top-color:var(--accent)}
.btn{min-height:36px;padding:7px 12px;border-color:var(--line);border-radius:4px;font-size:12px;font-weight:600}.field input,.field textarea,.field select{border-color:var(--line);font-size:13px}.bar-fill{background:var(--accent)}.code{overflow-wrap:anywhere}
.field label,.quiet,.nav-note,.kpi-note,.card p{font-size:12px}.field small,.message{font-size:12px;line-height:1.5}th,td{font-size:12px}th{font-size:11px}.status-badge,.destination .tag,.kpi-label{font-size:11px}.lock-box{border-radius:4px}.admin-optional summary{display:flex;flex-direction:column;gap:3px;cursor:pointer;list-style:none}.admin-optional summary::-webkit-details-marker{display:none}.admin-optional summary::before{content:"+";float:right;align-self:flex-end;position:absolute;color:var(--accent);font-size:20px}.admin-optional[open] summary::before{content:"−"}.admin-optional summary strong{font-size:16px}.admin-optional summary span{max-width:480px;color:var(--muted);font-size:12px}.admin-optional[open] summary{padding-bottom:14px;border-bottom:1px solid var(--line)}.admin-optional-body{padding-top:10px}.admin-optional{position:relative}.admin-optional summary::before{right:16px;top:10px}.admin-analysis-raw{margin-top:15px;border-top:1px solid var(--line)}.admin-analysis-raw summary{padding:10px 0;color:var(--muted);font-size:12px;font-weight:600;cursor:pointer}.admin-analysis-raw .actions{margin-top:4px}
@media(max-width:980px){.layout{grid-template-columns:1fr}nav{padding:10px 16px;background:var(--soft)}.nav-btn.active{box-shadow:inset 0 -3px 0 var(--accent)}main{padding:24px}}
@media(max-width:620px){main{padding:20px 14px}.topbar{height:56px}.layout{min-height:calc(100vh - 56px)}}
</style>
</head>
<body>
<section id="lock" class="lock-screen">
  <div class="lock-box">
    <div class="brand"><span class="mark"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><polygon points="5,11 19,11 21,14 15,14 15,18 18,22 6,22 9,18 9,14 5,14"/><polyline points="4,6 8,6 10,3 13,9 15,6 20,6"/></svg></span> EMA Forge</div>
    <h1>Study administration</h1>
    <p>Enter the private admin token created during Cloudflare deployment. It is retained only for this browser tab.</p>
    <div class="field"><label for="token">Admin token</label><input id="token" type="password" autocomplete="current-password" placeholder="At least 32 characters"></div>
    <button id="unlock" class="btn primary">Open study admin</button>
    <div id="lock-message" class="message" role="status"></div>
  </div>
</section>

<div id="app" class="shell hidden">
  <header class="topbar">
    <div class="brand"><span class="mark"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><polygon points="5,11 19,11 21,14 15,14 15,18 18,22 6,22 9,18 9,14 5,14"/><polyline points="4,6 8,6 10,3 13,9 15,6 20,6"/></svg></span> EMA Forge</div>
    <div id="study-name" class="study-name">Study admin</div>
    <div class="live-pill"><span id="live-dot" class="live-dot"></span><span id="live-label">Checking deployment</span></div>
    <button id="lock-admin" class="btn small">Lock</button>
  </header>
  <div class="layout">
    <nav aria-label="Study admin">
      <div class="nav-label">Study administration</div>
      <button class="nav-btn" data-view="settings">Install study</button>
      <button class="nav-btn active" data-view="overview">Overview</button>
      <button class="nav-btn" data-view="participants">Participants &amp; delivery</button>
      <button class="nav-btn" data-view="analyze">Analyze</button>
      <div class="nav-note">Participant data and credentials remain inside this Cloudflare deployment.</div>
    </nav>
    <main>
      <section id="view-overview" class="view active">
        <div class="page-head"><div><h1>Study overview</h1><p>Live deployment, storage, and collection status.</p></div><button class="btn" data-refresh>Refresh</button></div>
        <div id="overview-warning"></div>
        <div class="grid kpis">
          <div class="card"><div class="kpi-label">Stored sessions</div><div id="kpi-sessions" class="kpi-value">—</div><div class="kpi-note">Completed submissions</div></div>
          <div class="card"><div class="kpi-label">Participants observed</div><div id="kpi-participants" class="kpi-value">—</div><div class="kpi-note">Based on received data</div></div>
          <div class="card"><div class="kpi-label">Roster</div><div id="kpi-roster" class="kpi-value">—</div><div class="kpi-note">Active participants</div></div>
          <div class="card"><div class="kpi-label">Last response</div><div id="kpi-latest" class="kpi-value" style="font-size:1.1rem">—</div><div class="kpi-note">Server receipt time</div></div>
        </div>
        <div class="grid destinations">
          <article class="card destination"><div class="tag">Share this</div><h2>Participant study</h2><p>The live link participants open. Add identifiers through the roster or routing CSV.</p><div class="actions"><a id="participant-open" class="btn primary" target="_blank" rel="noopener">Open study</a><button class="btn copy-link" data-link="participant">Copy link</button></div></article>
          <article class="card destination"><div class="tag">Verify once</div><h2>Connection check</h2><p id="check-summary">Send a labeled synthetic submission. Setup checks never count as participant responses.</p><div class="actions"><a id="check-open" class="btn" target="_blank" rel="noopener">Run check</a></div></article>
          <article class="card destination"><div class="tag">Private</div><h2>Study administration</h2><p>Bookmark this page. Never share the admin token or administration URL with participants.</p><div class="actions"><button class="btn copy-link" data-link="admin">Copy admin link</button></div></article>
        </div>
        <div class="split">
          <section class="card"><h2>Collection status</h2><p id="collection-summary">Unlocking study data…</p><div id="window-bars" class="bars"></div></section>
          <section class="card"><h2>Messaging</h2><p id="messaging-summary">Checking Twilio configuration…</p><button class="btn" data-goto="participants">Manage participants and delivery</button></section>
        </div>
      </section>

      <section id="view-participants" class="view">
        <div class="page-head"><div><h1>Participants &amp; delivery</h1><p>Keep contact information separate from response files while scheduling individualized study links.</p></div><div class="actions"><button id="download-roster-template" class="btn">CSV template</button><label class="btn" for="roster-file">Import CSV</label><input id="roster-file" type="file" accept=".csv,text/csv" hidden></div></div>
        <div id="twilio-warning" class="warning hidden"></div>
        <div class="split">
          <section class="card">
            <h2>Cloudflare messaging</h2><p>The Worker checks due windows every five minutes and sends participant-specific links through the researcher's Twilio account.</p>
            <div class="field"><label><input id="messaging-enabled" type="checkbox"> Enable scheduled messaging</label></div>
            <div class="field"><label for="message-template">Message template</label><textarea id="message-template"></textarea><small>Available fields: <span class="code">{study}</span> <span class="code">{participant_id}</span> <span class="code">{day}</span> <span class="code">{window}</span> <span class="code">{link}</span></small></div>
            <div class="actions"><button id="save-messaging" class="btn primary">Save messaging settings</button></div><div id="messaging-message" class="message" role="status"></div>
          </section>
          <section class="card">
            <h2>Send a test message</h2><p>Confirm the Twilio credentials and sending number before enabling a live schedule.</p>
            <div class="field"><label for="test-phone">Test phone number</label><input id="test-phone" type="tel" placeholder="+15551234567"></div>
            <button id="send-test" class="btn">Send test SMS</button><div id="test-message" class="message" role="status"></div>
          </section>
        </div>
        <section class="card" style="margin-top:18px">
          <h2>Participant invite links</h2><p>Create an opaque, study-scoped link for a participant and session. The link hides routing details, follows the study response window, and can be revoked without changing the study URL. Scheduled Twilio messages use these links automatically.</p>
          <div class="inline-fields invite-fields">
            <div class="field"><label for="invite-pid">Participant</label><select id="invite-pid"></select></div>
            <div class="field"><label for="invite-day">Study day</label><input id="invite-day" type="number" min="1" value="1"></div>
            <div class="field"><label for="invite-session">Session</label><select id="invite-session"></select></div>
            <button id="create-invite" class="btn primary" style="margin-bottom:14px">Create link</button>
          </div>
          <div id="invite-message" class="message" role="status"></div>
          <div id="invite-table" class="table-wrap"><div class="empty">No participant links have been created.</div></div>
        </section>
        <section class="card" style="margin-top:18px">
          <h2>Study roster</h2><p>Use opaque participant IDs. Phone numbers must use E.164 format; timezones must be IANA names such as <span class="code">America/New_York</span>.</p>
          <div class="inline-fields">
            <div class="field"><label for="new-pid">Participant ID</label><input id="new-pid" placeholder="P001"></div>
            <div class="field"><label for="new-phone">Phone</label><input id="new-phone" type="tel" placeholder="+15551234567"></div>
            <div class="field"><label for="new-timezone">Timezone</label><input id="new-timezone" placeholder="America/New_York"></div>
            <div class="field"><label for="new-start">Start date</label><input id="new-start" type="date"></div>
            <button id="add-participant" class="btn" style="margin-bottom:14px">Add</button>
          </div>
          <div id="roster-table" class="table-wrap"><div class="empty">No participants have been added.</div></div>
          <div class="actions" style="margin-top:14px"><button id="save-roster" class="btn primary">Save roster</button></div><div id="roster-message" class="message" role="status"></div>
        </section>
      </section>

      <section id="view-analyze" class="view">
        <div class="page-head"><div><h1>Analyze live data</h1><p>Operational summaries are calculated in this browser from protected response and dispatch records.</p></div><div class="actions"><button id="load-analysis" class="btn">Refresh analysis</button></div></div>
        <section class="card" style="margin-bottom:18px;border-top:2px solid var(--accent)"><h2>Explore individual responses</h2><p>Download one analysis file with this study’s questions and responses, then open it in the detailed Analyze workspace. Select that one file when prompted; no separate configuration download is needed.</p><div class="actions"><button id="download-analysis-bundle" class="btn primary">Download analysis file</button><a class="btn" href="https://emaforge.keeganwhitacre.com/dashboard.html" target="_blank" rel="noopener noreferrer">Open detailed Analyze ↗</a></div><details class="admin-analysis-raw"><summary>Need the raw exports?</summary><div class="actions"><button id="download-responses" class="btn">Download raw NDJSON</button><button id="download-dispatch" class="btn">Download dispatch log</button></div></details></section>
        <div class="warning">These views are descriptive monitoring tools, not confirmatory statistical analyses. Missing response files alone are not treated as missed prompts.</div>
        <div class="grid kpis">
          <div class="card"><div class="kpi-label">Sessions</div><div id="analysis-sessions" class="kpi-value">—</div></div>
          <div class="card"><div class="kpi-label">Participants</div><div id="analysis-participants" class="kpi-value">—</div></div>
          <div class="card"><div class="kpi-label">Prompts dispatched</div><div id="analysis-dispatched" class="kpi-value">—</div><div id="analysis-completed-after-prompt" class="kpi-note">— matched completions</div></div>
          <div class="card"><div class="kpi-label">Physiology sessions</div><div id="analysis-physiology" class="kpi-value">—</div></div>
        </div>
        <div class="split">
          <section class="card"><h2>Observed sessions by window</h2><p>Counts only received, complete session envelopes.</p><div id="analysis-windows" class="bars"><div class="empty">Load analysis to inspect responses.</div></div></section>
          <section class="card"><h2>Delivery status</h2><p>Twilio status callbacks received by this Worker.</p><div id="analysis-delivery" class="bars"><div class="empty">No dispatch records loaded.</div></div></section>
        </div>
        <section class="card" style="margin-top:18px"><h2>Recent sessions</h2><div id="recent-table" class="table-wrap"><div class="empty">Load analysis to inspect responses.</div></div></section>
        <div id="analysis-message" class="message" role="status"></div>
      </section>

      <section id="view-settings" class="view">
        <div class="page-head"><div><p style="margin:0 0 7px;color:var(--accent);font-size:.72rem;font-weight:800;letter-spacing:.08em;text-transform:uppercase">First step</p><h1>Install your study</h1><p>Upload the prepared HTML from EMA Forge. This connects the participant experience to this deployment’s private storage.</p></div></div>
        <div class="setup-grid">
          <section class="card" style="border-top:3px solid var(--accent)"><h2>Install prepared study</h2><p>Choose the <span class="code">-cloudflare-study.html</span> file downloaded from Review &amp; Deploy.</p><div class="field"><label for="study-file">Prepared study HTML</label><input id="study-file" type="file" accept=".html,text/html"></div><button id="install-study" class="btn primary">Install and open overview</button><div id="install-message" class="message" role="status"></div></section>
          <section class="card"><h2>Data access</h2><p>Response and dispatch exports remain lossless researcher-controlled files.</p><div class="actions"><button class="btn" data-download="responses">Download responses</button><button class="btn" data-download="dispatch">Download dispatch log</button></div><p class="quiet">Keep raw exports in an institutionally approved encrypted location. This interface does not establish IRB, HIPAA, or institutional compliance by itself.</p></section>
          <details class="card admin-optional"><summary><strong>Optional SMS setup</strong><span id="twilio-summary">Connect Twilio when you need scheduled text invitations. Study installation works without it.</span></summary><div class="admin-optional-body"><div id="twilio-credentials-status" class="quiet" role="status">Checking SMS connection…</div><div class="field"><label for="twilio-sid">Account SID</label><input id="twilio-sid" autocomplete="off" placeholder="AC…"></div><div class="field"><label for="twilio-token">Auth Token</label><input id="twilio-token" type="password" autocomplete="new-password" placeholder="Paste your Twilio token"></div><div class="field"><label for="twilio-from">Sending phone number</label><input id="twilio-from" autocomplete="off" placeholder="+15551234567"></div><div class="field"><label for="twilio-service">Messaging Service SID (alternative)</label><input id="twilio-service" autocomplete="off" placeholder="MG…"></div><p class="quiet">Enter either a sending number or Messaging Service SID. Credentials are encrypted in private study storage using your admin token and are never shown again. If you change the admin token, reconnect Twilio.</p><div class="actions"><button id="save-twilio" class="btn primary">Connect Twilio</button><button id="remove-twilio" class="btn danger">Disconnect</button></div><div id="twilio-credentials-message" class="message" role="status"></div><p class="quiet">Set Twilio's incoming-message webhook to <span id="incoming-url" class="code"></span> using POST. Send a test from Participants &amp; delivery before enabling a schedule.</p></div></details>
          <section class="card"><h2>Security and retention</h2><p class="quiet">Use opaque participant IDs, protect this route with a unique token, consider Cloudflare Access for higher-risk studies, document retention and deletion procedures, and avoid collecting unnecessary identifiers.</p><button id="connection-settings" class="btn">Open connection check</button></section>
        </div>
      </section>
    </main>
  </div>
</div>

<script>
'use strict';
var tokenEl=document.getElementById('token');
var token=sessionStorage.getItem('ema_forge_admin_token')||'';
var overview=null;
var roster=[];
var inviteLinks=[];
var messaging={enabled:false,message_template:''};
tokenEl.value=token;

function auth(){return {'Authorization':'Bearer '+token};}
function byId(id){return document.getElementById(id);}
function setMessage(id,text,kind){var el=byId(id);el.textContent=text||'';el.className='message '+(kind||'');}
function text(id,value){byId(id).textContent=value==null?'—':String(value);}
function formatDate(value){if(!value)return 'None yet';var d=new Date(value);return isNaN(d)?String(value):d.toLocaleString();}
function formatCompactDate(value){if(!value)return 'None yet';var d=new Date(value);return isNaN(d)?String(value):d.toLocaleDateString(undefined,{month:'short',day:'numeric',hour:'numeric',minute:'2-digit'});}
async function api(path,options){options=options||{};options.headers=Object.assign({},options.headers||{},auth());var response=await fetch(path,options);if(response.status===401){lock();throw new Error('Admin token was not accepted.');}var contentType=response.headers.get('Content-Type')||'';var result=contentType.indexOf('application/json')>=0?await response.json():await response.text();if(!response.ok)throw new Error(result.error||result||'Request failed');return {response:response,result:result};}
function lock(){sessionStorage.removeItem('ema_forge_admin_token');token='';byId('app').classList.add('hidden');byId('lock').classList.remove('hidden');tokenEl.value='';}
async function unlock(){token=tokenEl.value.trim();if(token.length<32){setMessage('lock-message','Enter the complete token created during deployment.','bad');return;}setMessage('lock-message','Checking access…','');try{var data=(await api('/admin/status')).result;sessionStorage.setItem('ema_forge_admin_token',token);byId('lock').classList.add('hidden');byId('app').classList.remove('hidden');overview=data;renderOverview();showView(data.installed?'overview':'settings');await Promise.all([loadRoster(),loadMessaging(),loadInviteLinks(),loadTwilioStatus()]);}catch(error){setMessage('lock-message',error.message,'bad');}}

function showView(name){document.querySelectorAll('.view').forEach(function(el){el.classList.toggle('active',el.id==='view-'+name);});document.querySelectorAll('.nav-btn').forEach(function(el){el.classList.toggle('active',el.dataset.view===name);});if(name==='analyze'&&overview&&overview.response_count>0)loadAnalysis();}
function linkFor(kind){if(kind==='admin')return location.origin+'/admin';return location.origin+'/';}
async function copy(value,button){try{await navigator.clipboard.writeText(value);var previous=button.textContent;button.textContent='Copied';setTimeout(function(){button.textContent=previous;},1200);}catch(error){prompt('Copy this link:',value);}}
function barRows(containerId,counts){var container=byId(containerId);container.textContent='';var entries=Object.entries(counts||{}).sort(function(a,b){return b[1]-a[1];});if(!entries.length){container.innerHTML='<div class="empty">No records available yet.</div>';return;}var max=Math.max.apply(null,entries.map(function(row){return row[1];}));entries.forEach(function(row){var wrap=document.createElement('div');wrap.className='bar-row';var label=document.createElement('span');label.textContent=row[0];var track=document.createElement('div');track.className='bar-track';var fill=document.createElement('div');fill.className='bar-fill';fill.style.width=Math.max(3,row[1]/max*100)+'%';track.appendChild(fill);var count=document.createElement('strong');count.textContent=row[1];wrap.append(label,track,count);container.appendChild(wrap);});}

function renderOverview(){if(!overview)return;var installed=overview.installed;byId('live-dot').classList.toggle('on',installed);text('live-label',installed?'Study live':'Setup required');text('study-name',overview.study_name||'EMA Forge study');text('kpi-sessions',overview.response_count);text('kpi-participants',overview.observed_participants==null?'—':overview.observed_participants);text('kpi-roster',overview.active_roster_count);text('kpi-latest',formatCompactDate(overview.latest_response_at));byId('participant-open').href=overview.participant_url||location.origin+'/';byId('check-open').href=overview.connection_check_url||location.origin+'/check.html';text('check-summary',(overview.connection_check_count||0)+' setup check'+(overview.connection_check_count===1?'':'s')+' stored separately; none count as participant responses.');byId('incoming-url').textContent=location.origin+'/twilio/incoming';byId('connection-settings').onclick=function(){window.open(location.origin+'/check.html','_blank','noopener');};var warning=byId('overview-warning');warning.className=installed?'okbox':'warning';warning.textContent=installed?'Study installed. Run the connection check once, then complete one full participant session before enrollment.':'No participant study is installed yet. Use the first navigation item to upload the prepared Cloudflare study file.';text('collection-summary',overview.response_count?overview.response_count+' stored sessions across '+(overview.observed_participants||'unknown')+' observed participants.':'No participant sessions have been received yet.');barRows('window-bars',overview.sessions_by_window||{});renderMessagingSummary();}
async function refreshOverview(){try{overview=(await api('/admin/status')).result;renderOverview();}catch(error){setMessage('analysis-message',error.message,'bad');}}

async function loadRoster(){try{roster=(await api('/admin/roster')).result.participants||[];renderRoster();}catch(error){setMessage('roster-message',error.message,'bad');}}
function renderRoster(){var host=byId('roster-table');host.textContent='';renderInviteOptions();if(!roster.length){host.innerHTML='<div class="empty">No participants have been added.</div>';return;}var table=document.createElement('table');var head=document.createElement('thead');head.innerHTML='<tr><th>Participant</th><th>Phone</th><th>Timezone</th><th>Start date</th><th>Status</th><th></th></tr>';table.appendChild(head);var body=document.createElement('tbody');roster.forEach(function(person,index){var tr=document.createElement('tr');[person.participant_id,person.phone,person.timezone,person.start_date].forEach(function(value){var td=document.createElement('td');td.textContent=value;tr.appendChild(td);});var statusTd=document.createElement('td');var select=document.createElement('select');['active','inactive','opted_out'].forEach(function(value){var option=document.createElement('option');option.value=value;option.textContent=value.replace('_',' ');option.selected=person.status===value;select.appendChild(option);});select.onchange=function(){roster[index].status=select.value;renderInviteOptions();};statusTd.appendChild(select);tr.appendChild(statusTd);var action=document.createElement('td');var remove=document.createElement('button');remove.className='btn small danger';remove.textContent='Remove';remove.onclick=function(){roster.splice(index,1);renderRoster();};action.appendChild(remove);tr.appendChild(action);body.appendChild(tr);});table.appendChild(body);host.appendChild(table);}
function addParticipant(){var person={participant_id:byId('new-pid').value.trim(),phone:byId('new-phone').value.trim(),timezone:byId('new-timezone').value.trim(),start_date:byId('new-start').value,status:'active'};if(!person.participant_id||!person.phone||!person.timezone||!person.start_date){setMessage('roster-message','Complete all participant fields.','bad');return;}if(roster.some(function(row){return row.participant_id===person.participant_id;})){setMessage('roster-message','Participant IDs must be unique.','bad');return;}roster.push(person);['new-pid','new-phone'].forEach(function(id){byId(id).value='';});renderRoster();setMessage('roster-message','Participant added locally. Save the roster to apply changes.','good');}
async function saveRoster(){setMessage('roster-message','Saving roster…','');try{var result=(await api('/admin/roster',{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify({participants:roster})})).result;roster=result.participants;renderRoster();setMessage('roster-message','Roster saved.','good');await refreshOverview();}catch(error){setMessage('roster-message',error.message,'bad');}}
function parseCsv(raw){var rows=[];var row=[];var cell='';var quoted=false;for(var i=0;i<raw.length;i++){var c=raw[i];if(c==='"'){if(quoted&&raw[i+1]==='"'){cell+='"';i++;}else quoted=!quoted;}else if(c===','&&!quoted){row.push(cell);cell='';}else if((c==='\\n'||c==='\\r')&&!quoted){if(c==='\\r'&&raw[i+1]==='\\n')i++;row.push(cell);if(row.some(function(v){return v.trim();}))rows.push(row);row=[];cell='';}else cell+=c;}row.push(cell);if(row.some(function(v){return v.trim();}))rows.push(row);if(rows.length<2)return[];var headers=rows.shift().map(function(v){return v.trim().toLowerCase();});return rows.map(function(values){var item={};headers.forEach(function(header,index){item[header]=(values[index]||'').trim();});return item;});}
async function importRoster(file){try{var rows=parseCsv(await file.text());var imported=rows.map(function(row){var prefs=row.schedule_preferences_json||'';return {participant_id:row.participant_id,phone:row.phone,timezone:row.timezone,start_date:row.start_date,status:row.status||'active',schedule_preferences:prefs?JSON.parse(prefs):null};});if(!imported.length)throw new Error('The CSV did not contain participant rows.');roster=imported;renderRoster();setMessage('roster-message',imported.length+' participants imported locally. Review and save the roster.','good');}catch(error){setMessage('roster-message','Could not import CSV: '+error.message,'bad');}}
function downloadRosterTemplate(){downloadText('participant_id,phone,timezone,start_date,status,schedule_preferences_json\\nP001,+15551234567,America/New_York,'+new Date().toISOString().slice(0,10)+',active,\\n','ema-forge-roster-template.csv','text/csv');}

function renderInviteOptions(){var pid=byId('invite-pid'),session=byId('invite-session');var selectedPid=pid.value,selectedSession=session.value;pid.textContent='';roster.filter(function(person){return person.status==='active';}).forEach(function(person){var option=document.createElement('option');option.value=person.participant_id;option.textContent=person.participant_id;pid.appendChild(option);});session.textContent='';(overview&&overview.schedule_windows||[]).forEach(function(window){var option=document.createElement('option');option.value=window.id;option.textContent=window.label;session.appendChild(option);});if(selectedPid)pid.value=selectedPid;if(selectedSession)session.value=selectedSession;byId('invite-day').max=overview&&overview.study_days||1;byId('create-invite').disabled=!pid.value||!session.value;}
async function loadInviteLinks(){try{inviteLinks=(await api('/admin/invite-links')).result.links||[];renderInviteLinks();}catch(error){setMessage('invite-message',error.message,'bad');}}
function renderInviteLinks(){var host=byId('invite-table');host.textContent='';if(!inviteLinks.length){host.innerHTML='<div class="empty">No participant links have been created.</div>';return;}var table=document.createElement('table');table.innerHTML='<thead><tr><th>Participant</th><th>Day</th><th>Session</th><th>Link</th><th>Status</th><th></th></tr></thead>';var body=document.createElement('tbody');inviteLinks.slice(0,50).forEach(function(link){var row=document.createElement('tr');[link.participant_id,link.day,link.session_id].forEach(function(value){var td=document.createElement('td');td.textContent=value;row.appendChild(td);});var linkCell=document.createElement('td');var path=document.createElement('span');path.className='code';path.textContent='/j/'+link.token.slice(0,8)+'…';var copyButton=document.createElement('button');copyButton.className='btn small';copyButton.style.marginLeft='7px';copyButton.textContent='Copy';copyButton.onclick=function(){copy(link.short_url,copyButton);};linkCell.append(path,copyButton);row.appendChild(linkCell);var state=document.createElement('td');var badge=document.createElement('span');badge.className='status-badge '+(link.state==='active'?'good':'bad');badge.textContent=link.state;state.appendChild(badge);row.appendChild(state);var action=document.createElement('td');if(link.state==='active'){var revoke=document.createElement('button');revoke.className='btn small danger';revoke.textContent='Revoke';revoke.onclick=function(){revokeInvite(link.token);};action.appendChild(revoke);}row.appendChild(action);body.appendChild(row);});table.appendChild(body);host.appendChild(table);}
async function createInvite(){setMessage('invite-message','Creating protected participant link…','');try{var body={participant_id:byId('invite-pid').value,day:Number(byId('invite-day').value),session_id:byId('invite-session').value};var link=(await api('/admin/invite-links',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)})).result;inviteLinks.unshift(link);renderInviteLinks();setMessage('invite-message','Created '+link.short_url+'. Copy it from the table below.','good');}catch(error){setMessage('invite-message',error.message,'bad');}}
async function revokeInvite(inviteToken){try{await api('/admin/invite-links/'+encodeURIComponent(inviteToken),{method:'DELETE'});await loadInviteLinks();setMessage('invite-message','Participant link revoked.','good');}catch(error){setMessage('invite-message',error.message,'bad');}}

async function loadMessaging(){try{var result=(await api('/admin/messaging')).result;messaging=result.settings;messaging.configured=result.configured;byId('messaging-enabled').checked=!!messaging.enabled;byId('message-template').value=messaging.message_template||'';var warning=byId('twilio-warning');warning.classList.toggle('hidden',result.configured);warning.textContent=result.configured?'':'Twilio is not connected. Open Install study to enter your credentials, or distribute participant links yourself.';renderMessagingSummary();}catch(error){setMessage('messaging-message',error.message,'bad');}}
async function loadTwilioStatus(){try{var result=(await api('/admin/twilio-credentials')).result;text('twilio-credentials-status',result.configured?'Twilio connected'+(result.source==='cloudflare'?' through Cloudflare secrets.':' through this admin page.'):result.source==='admin'?'Stored credentials cannot be unlocked. Re-enter them after changing the admin token.':'Twilio is not connected. SMS is optional.');text('twilio-summary',result.configured?'Twilio connected. Expand to review settings.':result.source==='admin'?'Reconnect Twilio after changing the admin token.':'Connect Twilio for scheduled text invitations. Installation works without it.');byId('remove-twilio').disabled=result.source!=='admin';}catch(error){setMessage('twilio-credentials-message',error.message,'bad');}}
async function saveTwilioCredentials(){setMessage('twilio-credentials-message','Saving securely…','');try{await api('/admin/twilio-credentials',{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify({account_sid:byId('twilio-sid').value,auth_token:byId('twilio-token').value,from_number:byId('twilio-from').value,messaging_service_sid:byId('twilio-service').value})});['twilio-sid','twilio-token','twilio-from','twilio-service'].forEach(function(id){byId(id).value='';});setMessage('twilio-credentials-message','Connected. Send a test SMS from Participants & delivery before enabling scheduled messages.','good');await Promise.all([loadTwilioStatus(),loadMessaging(),refreshOverview()]);}catch(error){setMessage('twilio-credentials-message',error.message,'bad');}}
async function removeTwilioCredentials(){if(!confirm('Disconnect Twilio and pause scheduled SMS?'))return;try{await api('/admin/twilio-credentials',{method:'DELETE'});setMessage('twilio-credentials-message','Stored credentials removed and scheduled SMS paused.','good');await Promise.all([loadTwilioStatus(),loadMessaging(),refreshOverview()]);}catch(error){setMessage('twilio-credentials-message',error.message,'bad');}}
function renderMessagingSummary(){if(!overview)return;if(!messaging.configured){text('messaging-summary','Twilio is not connected. Study links can still be distributed manually.');return;}text('messaging-summary',messaging.enabled?'Scheduled SMS is enabled. '+(overview.dispatch_count||0)+' dispatch records are stored.':'Twilio is connected, but scheduled messaging is paused.');}
async function saveMessaging(){setMessage('messaging-message','Saving settings…','');try{var body={enabled:byId('messaging-enabled').checked,message_template:byId('message-template').value};var result=(await api('/admin/messaging',{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)})).result;messaging=Object.assign({},result.settings,{configured:result.configured});setMessage('messaging-message',messaging.enabled?'Scheduled messaging enabled.':'Messaging settings saved; automatic delivery is paused.','good');await refreshOverview();}catch(error){setMessage('messaging-message',error.message,'bad');}}
async function sendTest(){setMessage('test-message','Sending…','');try{var result=(await api('/admin/test-message',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({phone:byId('test-phone').value.trim()})})).result;setMessage('test-message','Twilio accepted test message '+result.message_sid+'. Check the handset and Twilio delivery status.','good');}catch(error){setMessage('test-message',error.message,'bad');}}

async function loadAllNdjson(path){var cursor='';var records=[];var pages=0;do{var response=await fetch(path+(cursor?'?cursor='+encodeURIComponent(cursor):''),{headers:auth()});if(response.status===401){lock();throw new Error('Admin token was not accepted.');}if(!response.ok){var problem=await response.json();throw new Error(problem.error||'Could not load records.');}var raw=await response.text();raw.split(/\\r?\\n/).forEach(function(line){if(line.trim())records.push(JSON.parse(line));});cursor=response.headers.get('X-EMA-Next-Cursor')||'';pages++;if(pages>100)throw new Error('Export exceeded the browser analysis limit. Download the raw data instead.');}while(cursor);return records;}
function sessionFromEnvelope(record){return record.session_data||record;}
function hasPhysiology(session){return (session.data||[]).some(function(entry){if(['epat','hct','heart_rate','hr_capture','ppg'].indexOf(entry.type)>=0)return true;if(entry.type==='ema_response')return Object.values(entry.responses||{}).some(function(response){return response&&response.value&&typeof response.value==='object'&&('bpm' in response.value||'ibi' in response.value||'ibis' in response.value);});return false;});}
async function loadAnalysis(){setMessage('analysis-message','Loading protected response and dispatch records…','');try{var results=await Promise.all([loadAllNdjson('/admin/export'),loadAllNdjson('/admin/dispatch-export')]);var responses=results[0],dispatches=results[1];var participants=new Set();var windows={};var physiology=0;var completedKeys=new Set();responses.forEach(function(record){var session=sessionFromEnvelope(record);var participant=String(session.participantId||record.participant_id||'Unknown');var day=record.day==null?session.day:record.day;participants.add(participant);var windowId=record.window_id||session.windowId||(session.phasePlan&&session.phasePlan[0]&&session.phasePlan[0].windowId)||'unknown';windows[windowId]=(windows[windowId]||0)+1;completedKeys.add(participant+'|'+day+'|'+windowId);if(hasPhysiology(session))physiology++;});var delivery={};dispatches.forEach(function(record){var state=record.state||'unknown';delivery[state]=(delivery[state]||0)+1;});var sentRecords=dispatches.filter(function(record){return !!record.message_sid||!!record.sent_at;});var matched=sentRecords.filter(function(record){return completedKeys.has(String(record.participant_id)+'|'+record.day+'|'+record.window_id);}).length;text('analysis-sessions',responses.length);text('analysis-participants',participants.size);text('analysis-dispatched',sentRecords.length);text('analysis-completed-after-prompt',matched+' matched completion'+(matched===1?'':'s'));text('analysis-physiology',physiology);barRows('analysis-windows',windows);barRows('analysis-delivery',delivery);renderRecent(responses);setMessage('analysis-message','Analysis refreshed at '+new Date().toLocaleTimeString()+'.','good');}catch(error){setMessage('analysis-message',error.message,'bad');}}
function renderRecent(records){var host=byId('recent-table');host.textContent='';if(!records.length){host.innerHTML='<div class="empty">No participant sessions have been received.</div>';return;}var sorted=records.slice().sort(function(a,b){return String((b.server_receipt||{}).received_at||'').localeCompare(String((a.server_receipt||{}).received_at||''));}).slice(0,50);var table=document.createElement('table');table.innerHTML='<thead><tr><th>Received</th><th>Participant</th><th>Day</th><th>Window</th><th>Session</th></tr></thead>';var body=document.createElement('tbody');sorted.forEach(function(record){var row=document.createElement('tr');var values=[formatDate((record.server_receipt||{}).received_at),record.participant_id,record.day,record.window_id,record.submission_id];values.forEach(function(value){var td=document.createElement('td');td.textContent=value==null?'—':value;row.appendChild(td);});body.appendChild(row);});table.appendChild(body);host.appendChild(table);}
function downloadText(content,name,type){var blob=new Blob([content],{type:type||'text/plain'});var link=document.createElement('a');link.href=URL.createObjectURL(blob);link.download=name;link.click();setTimeout(function(){URL.revokeObjectURL(link.href);},1000);}
async function downloadAnalysisBundle(){var button=byId('download-analysis-bundle');button.disabled=true;setMessage('analysis-message','Preparing study questions and responses…','');try{var results=await Promise.all([api('/admin/study-config'),loadAllNdjson('/admin/export')]);var config=results[0].result;var sessions=results[1];if(!sessions.length)throw new Error('No participant responses yet. Complete a study session before downloading an analysis file.');var bundle={format:'ema_forge_analysis_bundle',version:1,config:config,sessions:sessions};downloadText(JSON.stringify(bundle),'ema-forge-analysis.json','application/json');setMessage('analysis-message','Analysis file downloaded ('+sessions.length+' sessions). Open detailed Analyze and select this file.','good');}catch(error){setMessage('analysis-message',error.message,'bad');}finally{button.disabled=false;}}
async function downloadRecords(kind){var path=kind==='dispatch'?'/admin/dispatch-export':'/admin/export';var name=kind==='dispatch'?'ema-forge-dispatch.ndjson':'ema-forge-responses.ndjson';try{var records=await loadAllNdjson(path);downloadText(records.map(function(record){return JSON.stringify(record);}).join('\\n')+(records.length?'\\n':''),name,'application/x-ndjson');}catch(error){setMessage('analysis-message',error.message,'bad');}}
async function installStudy(){var file=byId('study-file').files[0];if(!file){setMessage('install-message','Choose the prepared study HTML file.','bad');return;}setMessage('install-message','Installing study…','');try{var result=(await api('/admin/install',{method:'POST',headers:{'Content-Type':'text/html'},body:await file.text()})).result;setMessage('install-message','Installed. Participant link: '+result.participant_url,'good');await refreshOverview();showView('overview');}catch(error){setMessage('install-message',error.message,'bad');}}

document.querySelectorAll('.nav-btn').forEach(function(button){button.onclick=function(){showView(button.dataset.view);};});
document.querySelectorAll('[data-goto]').forEach(function(button){button.onclick=function(){showView(button.dataset.goto);};});
document.querySelectorAll('[data-refresh]').forEach(function(button){button.onclick=refreshOverview;});
document.querySelectorAll('.copy-link').forEach(function(button){button.onclick=function(){copy(linkFor(button.dataset.link),button);};});
document.querySelectorAll('[data-download]').forEach(function(button){button.onclick=function(){downloadRecords(button.dataset.download);};});
byId('unlock').onclick=unlock;tokenEl.onkeydown=function(event){if(event.key==='Enter')unlock();};byId('lock-admin').onclick=lock;
byId('add-participant').onclick=addParticipant;byId('save-roster').onclick=saveRoster;byId('roster-file').onchange=function(){if(this.files[0])importRoster(this.files[0]);};byId('download-roster-template').onclick=downloadRosterTemplate;
byId('create-invite').onclick=createInvite;
byId('save-messaging').onclick=saveMessaging;byId('send-test').onclick=sendTest;byId('load-analysis').onclick=loadAnalysis;byId('download-analysis-bundle').onclick=downloadAnalysisBundle;byId('download-responses').onclick=function(){downloadRecords('responses');};byId('download-dispatch').onclick=function(){downloadRecords('dispatch');};byId('install-study').onclick=installStudy;
byId('save-twilio').onclick=saveTwilioCredentials;byId('remove-twilio').onclick=removeTwilioCredentials;
byId('new-timezone').value=Intl.DateTimeFormat().resolvedOptions().timeZone||'America/New_York';byId('new-start').value=new Date().toISOString().slice(0,10);
if(token)unlock();
</script>
</body>
</html>`;
